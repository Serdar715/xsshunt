package waf

import (
	"crypto/tls"
	"io"
	"net/http"
	"net/url"
	"regexp"
	"strings"
	"time"
)

// WAFSignature represents a WAF detection signature
type WAFSignature struct {
	Name           string
	HeaderPatterns []*regexp.Regexp
	CookiePatterns []*regexp.Regexp
	BodyPatterns   []*regexp.Regexp
	StatusCodes    []int
}

const (
	MaxBodySize = 50 * 1024 // 50KB limit for WAF detection
)

// Detector handles WAF detection with proxy and auth support
type Detector struct {
	client        *http.Client
	signatures    []WAFSignature
	cookies       string
	customHeaders map[string]string
}

// NewDetector creates a new WAF detector without proxy
func NewDetector() *Detector {
	return NewDetectorWithProxy("", "", nil)
}

// NewDetectorWithProxy creates a new WAF detector with optional proxy and auth support
func NewDetectorWithProxy(proxyURL, cookies string, headers map[string]string) *Detector {
	transport := &http.Transport{
		TLSClientConfig: &tls.Config{InsecureSkipVerify: true},
	}

	// Configure proxy if provided
	if proxyURL != "" {
		proxyURLParsed, err := url.Parse(proxyURL)
		if err == nil {
			transport.Proxy = http.ProxyURL(proxyURLParsed)
		}
	}

	return &Detector{
		client: &http.Client{
			Timeout:   15 * time.Second,
			Transport: transport,
			CheckRedirect: func(req *http.Request, via []*http.Request) error {
				return http.ErrUseLastResponse
			},
		},
		signatures:    getWAFSignatures(),
		cookies:       cookies,
		customHeaders: headers,
	}
}

// getWAFSignatures returns comprehensive WAF detection signatures with compiled regexes
func getWAFSignatures() []WAFSignature {
	// Helper to compile patterns
	compile := func(patterns []string) []*regexp.Regexp {
		var compiled []*regexp.Regexp
		for _, p := range patterns {
			re, err := regexp.Compile("(?i)" + p) // Case insensitive by default
			if err == nil {
				compiled = append(compiled, re)
			}
		}
		return compiled
	}

	return []WAFSignature{
		{
			Name: "cloudflare",
			HeaderPatterns: compile([]string{
				"cf-ray",
				"cf-request-id",
				"cf-cache-status",
				"cloudflare",
				"__cfduid",
				"cf-connecting-ip",
			}),
			CookiePatterns: compile([]string{
				"__cfduid",
				"cf_clearance",
				"cf_use_ob",
			}),
			BodyPatterns: compile([]string{
				"cloudflare",
				"attention required",
				"cf-browser-verification",
				"ray id:",
				"cloudflare ray id",
			}),
			StatusCodes: []int{403, 503},
		},
		{
			Name: "akamai",
			HeaderPatterns: compile([]string{
				"akamai",
				"akamai-",
				"x-akamai-",
				"akamaighost",
				"akamai-origin-hop",
			}),
			CookiePatterns: compile([]string{
				"akavpau_",
				"akamai_",
				"ak_bmsc",
				"bm_sv",
				"bm_sz",
			}),
			BodyPatterns: compile([]string{
				"akamai",
				"access denied",
				"reference#",
			}),
			StatusCodes: []int{403},
		},
		{
			Name: "cloudfront",
			HeaderPatterns: compile([]string{
				"x-amz-",
				"x-amzn-",
				"cloudfront",
				"x-cache",
				"via.*cloudfront",
			}),
			CookiePatterns: compile([]string{
				"awsalb",
				"awsalbcors",
				"x-amz-",
			}),
			BodyPatterns: compile([]string{
				"cloudfront",
				"amazon cloudfront",
				"generated by cloudfront",
			}),
			StatusCodes: []int{403},
		},
		{
			Name: "imperva",
			HeaderPatterns: compile([]string{
				"incapsula",
				"x-iinfo",
				"x-cdn",
				"imperva",
			}),
			CookiePatterns: compile([]string{
				"incap_ses",
				"visid_incap",
				"nlbi_",
				"incap_ses_",
			}),
			BodyPatterns: compile([]string{
				"incapsula",
				"imperva",
				"incap_ses",
				"request unsuccessful",
				"blocked by imperva",
			}),
			StatusCodes: []int{403},
		},
		{
			Name: "wordfence",
			HeaderPatterns: compile([]string{
				"x-wordfence",
			}),
			CookiePatterns: compile([]string{
				"wfvt_",
				"wordfence",
			}),
			BodyPatterns: compile([]string{
				"wordfence",
				"generated by wordfence",
				"your access to this site has been limited",
				"block reason:",
			}),
			StatusCodes: []int{403, 503},
		},
		{
			Name: "modsecurity",
			HeaderPatterns: compile([]string{
				"mod_security",
				"modsecurity",
				"nyob",
			}),
			CookiePatterns: compile([]string{}),
			BodyPatterns: compile([]string{
				"mod_security",
				"modsecurity",
				"not acceptable",
				"this error was generated by mod_security",
				"rules error",
			}),
			StatusCodes: []int{403, 406},
		},
		{
			Name: "sucuri",
			HeaderPatterns: compile([]string{
				"x-sucuri-",
				"sucuri",
				"x-sucuri-id",
				"x-sucuri-cache",
			}),
			CookiePatterns: compile([]string{
				"sucuri_cloudproxy",
			}),
			BodyPatterns: compile([]string{
				"sucuri",
				"sucuri cloudproxy",
				"access denied - sucuri",
				"sucuri websitesecurity",
			}),
			StatusCodes: []int{403},
		},
		{
			Name: "f5",
			HeaderPatterns: compile([]string{
				"bigip",
				"f5",
				"x-waf-",
				"ts",
				"x-cnection",
			}),
			CookiePatterns: compile([]string{
				"bigipserver",
				"f5_",
				"ts",
				"lastmrh_sess",
				"mrhhits",
			}),
			BodyPatterns: compile([]string{
				"bigip",
				"big-ip",
				"f5 networks",
				"the requested url was rejected",
			}),
			StatusCodes: []int{403},
		},
		{
			Name: "barracuda",
			HeaderPatterns: compile([]string{
				"barracuda",
				"barra_counter_session",
			}),
			CookiePatterns: compile([]string{
				"barra_counter_session",
				"bni_",
			}),
			BodyPatterns: compile([]string{
				"barracuda",
				"barracuda networks",
				"you have been blocked",
			}),
			StatusCodes: []int{403},
		},
		{
			Name: "aws-waf",
			HeaderPatterns: compile([]string{
				"x-amzn-waf",
				"awswaf",
			}),
			CookiePatterns: compile([]string{
				"aws-waf-token",
			}),
			BodyPatterns: compile([]string{
				"aws waf",
				"request blocked",
			}),
			StatusCodes: []int{403},
		},
		{
			Name: "fortiweb",
			HeaderPatterns: compile([]string{
				"fortiwafsid",
			}),
			CookiePatterns: compile([]string{
				"fortiwafsid",
				"cookiesession1",
			}),
			BodyPatterns: compile([]string{
				"fortinet",
				"fortiweb",
				"fgd_icon",
			}),
			StatusCodes: []int{403},
		},
		{
			Name: "comodo",
			HeaderPatterns: compile([]string{
				"x-cw-",
			}),
			CookiePatterns: compile([]string{
				"cprefix",
			}),
			BodyPatterns: compile([]string{
				"comodo",
				"protected by comodo",
			}),
			StatusCodes: []int{403},
		},
	}
}

// Detect identifies the WAF protecting the target
func (d *Detector) Detect(targetURL string) (string, error) {
	// First, try a clean request to get baseline
	baselineResp, err := d.makeRequest(targetURL)
	if err != nil {
		return "", err
	}
	defer baselineResp.Body.Close()

	baselineBody, _ := io.ReadAll(io.LimitReader(baselineResp.Body, MaxBodySize))

	// Check baseline for WAF signatures
	if waf := d.matchSignatures(baselineResp.Header, baselineResp.Cookies(), string(baselineBody), baselineResp.StatusCode); waf != "" {
		return waf, nil
	}

	// Send probe requests with attack payloads to trigger WAF
	probePayloads := []string{
		"<script>alert(1)</script>",
		"' OR 1=1--",
		"../../../etc/passwd",
		"${7*7}",
		"{{7*7}}",
		"<img src=x onerror=alert(1)>",
	}

	for _, payload := range probePayloads {
		probeURL := targetURL + url.QueryEscape(payload)
		probeResp, err := d.makeRequest(probeURL)
		if err != nil {
			continue
		}

		probeBody, _ := io.ReadAll(io.LimitReader(probeResp.Body, MaxBodySize))
		probeResp.Body.Close()

		// Check probe response for WAF signatures
		if waf := d.matchSignatures(probeResp.Header, probeResp.Cookies(), string(probeBody), probeResp.StatusCode); waf != "" {
			return waf, nil
		}

		// Check for status code changes that might indicate WAF
		if probeResp.StatusCode == 403 || probeResp.StatusCode == 406 || probeResp.StatusCode == 429 || probeResp.StatusCode == 503 {
			if baselineResp.StatusCode != probeResp.StatusCode {
				// WAF likely blocking, but can't identify which one
				return "unknown", nil
			}
		}
	}

	return "", nil
}

// makeRequest sends an HTTP request with authentication and custom headers
func (d *Detector) makeRequest(targetURL string) (*http.Response, error) {
	req, err := http.NewRequest("GET", targetURL, nil)
	if err != nil {
		return nil, err
	}

	// Set standard headers
	req.Header.Set("User-Agent", "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36")
	req.Header.Set("Accept", "text/html,application/xhtml+xml,application/xml;q=0.9,image/webp,*/*;q=0.8")
	req.Header.Set("Accept-Language", "en-US,en;q=0.5")
	req.Header.Set("Connection", "keep-alive")

	// Set cookies if provided
	if d.cookies != "" {
		req.Header.Set("Cookie", d.cookies)
	}

	// Set custom headers if provided
	for key, value := range d.customHeaders {
		req.Header.Set(key, value)
	}

	return d.client.Do(req)
}

// matchSignatures checks response against all WAF signatures
func (d *Detector) matchSignatures(headers http.Header, cookies []*http.Cookie, body string, statusCode int) string {
	// Optimization: Pre-check status code matches to avoid expensive regex if possible
	// However, some WAFs don't block by status code always, so we run full check.
	// We can add logic here to quick-fail if status code is 200 and WAF usually blocks with 403,
	// but let's stick to correctness for now.

	for _, sig := range d.signatures {
		// Check headers
		for name, values := range headers {
			headerStr := name + ": " + strings.Join(values, ", ")
			for _, re := range sig.HeaderPatterns {
				if re.MatchString(headerStr) {
					return sig.Name
				}
			}
		}

		// Check cookies
		for _, cookie := range cookies {
			cookieStr := cookie.Name + "=" + cookie.Value
			for _, re := range sig.CookiePatterns {
				if re.MatchString(cookieStr) {
					return sig.Name
				}
			}
		}

		// Check body
		for _, re := range sig.BodyPatterns {
			if re.MatchString(body) {
				return sig.Name
			}
		}
	}

	return ""
}
